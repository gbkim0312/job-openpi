# 채용 공고 통합 수집·관리 OpenAPI 서버 구현 지침

## 1. 프로젝트 개요

프로젝트명은 특정 채용 플랫폼에 종속되지 않도록 다음으로 한다.

```text
job-collector
```

이 프로젝트는 원티드, 사람인, 잡코리아 등 여러 채용 플랫폼의 공고를 수집하고 공통 형식으로 정규화하여 **REST OpenAPI로 제공하는 독립 서버**다.

관리자는 웹 대시보드에서 다음을 수행할 수 있어야 한다.

* 활성 채용 공고 조회
* 신규·변경·마감 공고 확인
* 수집 실행 상태와 오류 확인
* 출처별 동작 상태 확인
* 검색 프로필 확인
* 수동 동기화 실행
* 특정 공고 상태 재확인
* 공고 변경 이력 확인

현재 단계에서는 다음을 구현하지 않는다.

```text
LLM 호출
리서치 도구 연동
OpenWebUI 연동
프롬프트 생성
뉴스·채용 보고서 생성
Nextcloud Talk 또는 이메일 전송
```

향후 외부 도구가 OpenAPI를 호출할 수 있도록 범용적인 조회 API만 제공한다.

---

# 2. 핵심 목표

## 필수 기능

1. 원티드 채용 공고 수집
2. 사람인·잡코리아를 어댑터 추가로 확장할 수 있는 헥사고날 구조
3. 공고 상태 정규화
4. 공고 변경 이력 저장
5. 관심 분야 검색 프로필 지원
6. OpenAPI 기반 공고 조회 API
7. 관리자용 동기화 API
8. 웹 대시보드
9. 예약 수집 스케줄러
10. Docker Compose 기반 실행
11. PostgreSQL 영속화
12. 파서 fixture 테스트와 어댑터 계약 테스트

## 관심 분야 기본 프로필

초기 기본 프로필은 다음 분야를 대상으로 한다.

```text
모빌리티
SDV
차량용 소프트웨어
차량 보안
V2X
PKI·암호
C++
Linux
임베디드
네트워크
미들웨어
AUTOSAR
```

---

# 3. 전체 아키텍처

```text
원티드 ───── WantedAdapter ─────┐
사람인 ───── SaraminAdapter ────┼─ 정규화 ─ 변경 감지 ─ PostgreSQL
잡코리아 ─── JobKoreaAdapter ───┘                    │
                                                     ├─ FastAPI OpenAPI
                                                     ├─ 관리자 대시보드
                                                     └─ Scheduler
```

## 실행 서비스

Docker Compose에는 다음 서비스를 둔다.

```text
postgres
migrate
api
scheduler
dashboard
```

### 역할

| 서비스         | 역할                  |
| ----------- | ------------------- |
| `postgres`  | 공고, 변경 이력, 수집 기록 저장 |
| `migrate`   | Alembic 마이그레이션 수행   |
| `api`       | 조회·관리 OpenAPI 제공    |
| `scheduler` | 정기 수집 및 활성 공고 재검증   |
| `dashboard` | 관리자 웹 대시보드          |

대시보드가 PostgreSQL에 직접 접근하면 안 된다.

```text
Dashboard → REST OpenAPI → PostgreSQL
```

---

# 4. 기술 스택

## 백엔드

```text
Python 3.13
FastAPI
Pydantic v2
Pydantic Settings
SQLAlchemy 2.x Async
Alembic
PostgreSQL
asyncpg
httpx
BeautifulSoup4
lxml
APScheduler
tenacity
structlog
PyYAML
```

## 프론트엔드

```text
React
TypeScript
Vite
React Router
TanStack Query
TanStack Table
Tailwind CSS
shadcn/ui
Recharts
```

## 테스트와 품질

```text
pytest
pytest-asyncio
respx
ruff
mypy
Vitest
React Testing Library
Playwright E2E
```

---

# 5. 아키텍처 규칙

다음 규칙을 반드시 지킨다.

```text
domain은 FastAPI, SQLAlchemy, httpx를 참조하지 않는다.
application은 HTML 구조와 사이트별 응답 형식을 알지 못한다.
사이트별 구현은 outbound source adapter에 격리한다.
공개 조회 API는 외부 채용 사이트를 직접 호출하지 않는다.
조회 API는 저장된 DB 데이터만 반환한다.
수집 실패가 조회 API 장애로 이어지지 않게 한다.
한 채용 사이트가 실패해도 다른 사이트 수집은 계속한다.
UNKNOWN 상태를 ACTIVE로 취급하지 않는다.
검색 목록에서 사라졌다는 이유만으로 CLOSED 처리하지 않는다.
사이트별 원본 데이터와 공통 도메인 모델을 분리한다.
```

도메인 계층에서 `Wanted`라는 이름은 `JobSource.WANTED` 외에는 사용하지 않는다.

---

# 6. 프로젝트 구조

```text
job-collector/
├── backend/
│   ├── src/
│   │   └── job_collector/
│   │       ├── domain/
│   │       │   ├── model/
│   │       │   │   ├── job_posting.py
│   │       │   │   ├── job_snapshot.py
│   │       │   │   ├── crawl_run.py
│   │       │   │   ├── company.py
│   │       │   │   ├── experience.py
│   │       │   │   ├── location.py
│   │       │   │   ├── deadline.py
│   │       │   │   ├── source.py
│   │       │   │   ├── status.py
│   │       │   │   └── category.py
│   │       │   ├── service/
│   │       │   │   ├── normalizer.py
│   │       │   │   ├── status_resolver.py
│   │       │   │   ├── category_classifier.py
│   │       │   │   ├── content_hasher.py
│   │       │   │   ├── change_detector.py
│   │       │   │   └── duplicate_detector.py
│   │       │   └── exception.py
│   │       │
│   │       ├── application/
│   │       │   ├── port/
│   │       │   │   ├── inbound/
│   │       │   │   │   ├── search_jobs_use_case.py
│   │       │   │   │   ├── sync_source_use_case.py
│   │       │   │   │   ├── sync_all_sources_use_case.py
│   │       │   │   │   ├── recheck_job_use_case.py
│   │       │   │   │   └── get_dashboard_use_case.py
│   │       │   │   └── outbound/
│   │       │   │       ├── job_source_port.py
│   │       │   │       ├── job_repository_port.py
│   │       │   │       ├── snapshot_repository_port.py
│   │       │   │       ├── crawl_run_repository_port.py
│   │       │   │       └── clock_port.py
│   │       │   ├── use_case/
│   │       │   │   ├── sync_source.py
│   │       │   │   ├── sync_all_sources.py
│   │       │   │   ├── recheck_job.py
│   │       │   │   ├── search_jobs.py
│   │       │   │   ├── get_job_detail.py
│   │       │   │   ├── get_job_changes.py
│   │       │   │   ├── get_crawl_runs.py
│   │       │   │   └── get_dashboard_summary.py
│   │       │   └── dto/
│   │       │       ├── job_query.py
│   │       │       ├── sync_command.py
│   │       │       ├── sync_result.py
│   │       │       └── dashboard_summary.py
│   │       │
│   │       ├── adapter/
│   │       │   ├── inbound/
│   │       │   │   ├── http/
│   │       │   │   │   ├── app.py
│   │       │   │   │   ├── dependencies.py
│   │       │   │   │   ├── authentication.py
│   │       │   │   │   ├── error_handler.py
│   │       │   │   │   ├── middleware.py
│   │       │   │   │   ├── routers/
│   │       │   │   │   │   ├── health.py
│   │       │   │   │   │   ├── jobs.py
│   │       │   │   │   │   ├── dashboard.py
│   │       │   │   │   │   ├── sources.py
│   │       │   │   │   │   ├── profiles.py
│   │       │   │   │   │   └── admin.py
│   │       │   │   │   └── schemas/
│   │       │   │   │       ├── job.py
│   │       │   │   │       ├── dashboard.py
│   │       │   │   │       ├── source.py
│   │       │   │   │       └── admin.py
│   │       │   │   └── scheduler/
│   │       │   │       └── sync_scheduler.py
│   │       │   │
│   │       │   └── outbound/
│   │       │       ├── source/
│   │       │       │   ├── common/
│   │       │       │   │   ├── models.py
│   │       │       │   │   ├── retry.py
│   │       │       │   │   ├── rate_limiter.py
│   │       │       │   │   └── http_client.py
│   │       │       │   ├── wanted/
│   │       │       │   │   ├── adapter.py
│   │       │       │   │   ├── client.py
│   │       │       │   │   ├── parser.py
│   │       │       │   │   ├── mapper.py
│   │       │       │   │   ├── status_parser.py
│   │       │       │   │   └── models.py
│   │       │       │   ├── saramin/
│   │       │       │   │   ├── adapter.py
│   │       │       │   │   ├── client.py
│   │       │       │   │   ├── mapper.py
│   │       │       │   │   └── models.py
│   │       │       │   └── jobkorea/
│   │       │       │       └── README.md
│   │       │       └── persistence/
│   │       │           ├── database.py
│   │       │           ├── models.py
│   │       │           ├── job_repository.py
│   │       │           ├── snapshot_repository.py
│   │       │           └── crawl_run_repository.py
│   │       │
│   │       ├── bootstrap/
│   │       │   ├── container.py
│   │       │   ├── settings.py
│   │       │   └── logging.py
│   │       └── main.py
│   │
│   ├── migrations/
│   ├── profiles/
│   │   └── mobility_sdv_security_cpp.yml
│   ├── tests/
│   │   ├── unit/
│   │   ├── contract/
│   │   ├── integration/
│   │   └── fixtures/
│   │       └── wanted/
│   ├── scripts/
│   │   └── entrypoint.sh
│   ├── pyproject.toml
│   ├── alembic.ini
│   └── Dockerfile
│
├── dashboard/
│   ├── src/
│   │   ├── api/
│   │   ├── components/
│   │   ├── features/
│   │   │   ├── overview/
│   │   │   ├── jobs/
│   │   │   ├── crawl-runs/
│   │   │   ├── sources/
│   │   │   ├── profiles/
│   │   │   └── settings/
│   │   ├── layouts/
│   │   ├── pages/
│   │   ├── routes/
│   │   ├── types/
│   │   └── main.tsx
│   ├── nginx/
│   │   └── default.conf
│   ├── package.json
│   ├── package-lock.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── Dockerfile
│
├── data/
├── compose.yml
├── .env.example
├── Makefile
└── README.md
```

---

# 7. 핵심 도메인 모델

## 출처

```python
from enum import StrEnum


class JobSource(StrEnum):
    WANTED = "WANTED"
    SARAMIN = "SARAMIN"
    JOBKOREA = "JOBKOREA"
```

## 공고 상태

```python
from enum import StrEnum


class JobStatus(StrEnum):
    ACTIVE = "ACTIVE"
    CLOSED = "CLOSED"
    UNKNOWN = "UNKNOWN"
    DELETED = "DELETED"
```

의미:

| 상태        | 의미                       |
| --------- | ------------------------ |
| `ACTIVE`  | 현재 지원 가능한 상태가 확인됨        |
| `CLOSED`  | 명시적으로 마감됨                |
| `UNKNOWN` | 페이지는 있으나 현재 상태를 확정할 수 없음 |
| `DELETED` | 삭제 또는 404가 확인됨           |

## 변경 유형

```python
class JobChangeType(StrEnum):
    CREATED = "CREATED"
    CONTENT_UPDATED = "CONTENT_UPDATED"
    STATUS_CHANGED = "STATUS_CHANGED"
    CLOSED = "CLOSED"
    REOPENED = "REOPENED"
    DELETED = "DELETED"
```

## 카테고리

```python
class JobCategory(StrEnum):
    MOBILITY = "MOBILITY"
    SDV = "SDV"
    AUTOMOTIVE_SECURITY = "AUTOMOTIVE_SECURITY"
    V2X = "V2X"
    AUTOSAR = "AUTOSAR"
    EMBEDDED = "EMBEDDED"
    CPP = "CPP"
    LINUX = "LINUX"
    NETWORK = "NETWORK"
    PKI_CRYPTO = "PKI_CRYPTO"
    MIDDLEWARE = "MIDDLEWARE"
    ROBOTICS = "ROBOTICS"
```

## 공고 엔티티

```python
from dataclasses import dataclass, field
from datetime import date, datetime
from uuid import UUID


@dataclass
class JobPosting:
    id: UUID
    source: JobSource
    source_job_id: str
    canonical_url: str

    company_name: str
    title: str

    detected_status: JobStatus
    manual_status_override: JobStatus | None
    source_status: str | None
    status_reason: str | None

    location_raw: str | None
    region: str | None
    city: str | None

    experience_raw: str | None
    min_experience_years: int | None
    max_experience_years: int | None

    employment_type: str | None

    deadline_raw: str | None
    deadline_date: date | None
    always_open: bool

    responsibilities: list[str] = field(default_factory=list)
    requirements: list[str] = field(default_factory=list)
    preferred_qualifications: list[str] = field(default_factory=list)

    skills: set[str] = field(default_factory=set)
    categories: set[JobCategory] = field(default_factory=set)

    posted_at: datetime | None = None
    first_seen_at: datetime | None = None
    last_seen_at: datetime | None = None
    last_checked_at: datetime | None = None
    closed_at: datetime | None = None

    content_hash: str = ""

    @property
    def effective_status(self) -> JobStatus:
        return self.manual_status_override or self.detected_status
```

MVP에서는 수동 상태 변경 UI를 생략할 수 있지만 DB 구조는 확장 가능하게 둔다.

---

# 8. 외부 원본 모델

사이트 어댑터는 도메인 엔티티를 직접 생성하지 않고 공통 원본 모델을 반환한다.

```python
from dataclasses import dataclass
from datetime import datetime


@dataclass(frozen=True)
class SourceJobReference:
    source: JobSource
    source_job_id: str
    url: str


@dataclass(frozen=True)
class SourceJobPosting:
    source: JobSource
    source_job_id: str
    url: str

    raw_title: str
    raw_company: str | None
    raw_location: str | None
    raw_experience: str | None
    raw_employment_type: str | None
    raw_deadline: str | None
    raw_status: str | None

    responsibilities: tuple[str, ...]
    requirements: tuple[str, ...]
    preferred_qualifications: tuple[str, ...]
    skills: tuple[str, ...]

    posted_at: datetime | None
    fetched_at: datetime

    raw_payload: dict[str, object]
```

`raw_payload`에는 파싱에 사용된 구조화 데이터만 저장한다. HTML 전체는 기본적으로 DB에 저장하지 않는다.

---

# 9. 사이트 어댑터 포트

```python
from abc import ABC, abstractmethod
from collections.abc import Sequence
from dataclasses import dataclass


@dataclass(frozen=True)
class SourceSearchQuery:
    query: str
    page: int = 1
    page_size: int = 20


@dataclass(frozen=True)
class SourceCapabilities:
    supports_keyword_search: bool
    supports_incremental_sync: bool
    supports_status_check: bool
    supports_posted_date: bool
    supports_deadline: bool


class JobSourcePort(ABC):
    @property
    @abstractmethod
    def source(self) -> JobSource:
        raise NotImplementedError

    @property
    @abstractmethod
    def capabilities(self) -> SourceCapabilities:
        raise NotImplementedError

    @abstractmethod
    async def search(
        self,
        query: SourceSearchQuery,
    ) -> Sequence[SourceJobReference]:
        raise NotImplementedError

    @abstractmethod
    async def fetch_detail(
        self,
        reference: SourceJobReference,
    ) -> SourceJobPosting:
        raise NotImplementedError
```

추후 다음 어댑터가 같은 포트를 구현해야 한다.

```python
class WantedJobSourceAdapter(JobSourcePort):
    ...

class SaraminJobSourceAdapter(JobSourcePort):
    ...

class JobKoreaJobSourceAdapter(JobSourcePort):
    ...
```

---

# 10. 검색 프로필

검색 키워드를 코드에 하드코딩하지 않는다.

`backend/profiles/mobility_sdv_security_cpp.yml`

```yaml
id: mobility_sdv_security_cpp
display_name: 모빌리티·SDV·보안·C++ 개발자

queries:
  - 모빌리티 C++ 개발자
  - 차량 소프트웨어 C++
  - SDV AUTOSAR 개발자
  - 차량 플랫폼 C++ 개발자
  - 차량 보안 C++ 개발자
  - V2X 보안 개발자
  - 임베디드 보안 개발자
  - Linux 시스템 C++ 개발자
  - PKI 암호 C++ 개발자

include_keywords:
  - C++
  - C/C++
  - 모빌리티
  - 자동차
  - 차량
  - SDV
  - Software Defined Vehicle
  - AUTOSAR
  - Adaptive AUTOSAR
  - V2X
  - C-V2X
  - PKI
  - 암호
  - HSM
  - Secure Boot
  - Linux
  - 임베디드
  - SOME/IP
  - DDS
  - CAN
  - Ethernet
  - 미들웨어
  - SDK

exclude_keywords:
  - 영업
  - 마케팅
  - 사업개발
  - 강사
  - 게임 클라이언트
  - 퀀트
  - 보안 관제
  - 개인정보보호 담당
  - 컴플라이언스 전담

source_queries:
  WANTED:
    - 모빌리티 차량 소프트웨어 C++ 개발자
    - SDV AUTOSAR 차량 플랫폼 개발자
    - 차량 임베디드 보안 PKI C++ 개발자
    - Linux 임베디드 시스템 C++ 개발자

  SARAMIN:
    - 모빌리티 C++ 개발자
    - SDV AUTOSAR 개발자
    - 차량 보안 C++ 개발자
    - 임베디드 Linux C++ 개발자
```

프로필은 Pydantic 모델로 검증한다.

기본 프로필 YAML은 최초 마이그레이션 후 DB에 시드하는 템플릿이다. 실행 중에는 PostgreSQL의 `search_profiles`가 원본이며, 대시보드에서 생성·수정·삭제할 수 있다. YAML을 다시 읽는 reload는 누락된 기본 프로필만 추가하며 대시보드에서 수정한 DB 프로필을 덮어쓰지 않는다.

---

# 11. 원티드 어댑터 구현

## 파싱 우선순위

```text
1. 공개된 구조화 JSON
2. JSON-LD JobPosting
3. 서버 렌더링 HTML
4. 선택적 브라우저 자동화
```

Playwright는 초기 의존성에 포함하지 않는다. 일반 HTTP 방식으로 필요한 데이터가 확보되지 않을 때만 별도 옵션으로 추가한다.

## 파싱 대상

```text
원티드 공고 ID
공고 URL
회사명
포지션명
지역
경력
고용 형태
게시일
마감일
주요 업무
자격요건
우대사항
기술 스택
모집 상태
지원 가능 여부
```

## 모집 상태 판정 우선순위

```text
1. 명시적인 종료 문구
2. 명시적인 활성 상태
3. 마감 날짜
4. 지원 버튼 또는 지원 동작
5. UNKNOWN
```

예시:

```python
def resolve_status(
    raw_status: str | None,
    deadline_date: date | None,
    always_open: bool,
    has_apply_action: bool,
    current_date: date,
) -> tuple[JobStatus, str]:
    normalized = (raw_status or "").strip().lower()

    closed_markers = (
        "채용 마감",
        "접수 마감",
        "지원 종료",
        "모집 종료",
        "공고 종료",
    )

    if any(marker in normalized for marker in closed_markers):
        return JobStatus.CLOSED, "explicit closed marker"

    if deadline_date is not None and deadline_date < current_date:
        return JobStatus.CLOSED, "deadline passed"

    if always_open and has_apply_action:
        return JobStatus.ACTIVE, "always open and apply action available"

    if has_apply_action:
        return JobStatus.ACTIVE, "apply action available"

    return JobStatus.UNKNOWN, "status could not be verified"
```

## 요청 정책

```text
기본 동시 요청 수: 2
요청 간 최소 간격: 1.5초
HTTP 타임아웃: 20초
재시도 횟수: 3회
```

재시도 대상:

```text
네트워크 연결 실패
408
429
500
502
503
504
```

재시도하지 않는 대상:

```text
400
401
403
404
파서 검증 오류
```

다음 기능은 구현하지 않는다.

```text
CAPTCHA 우회
IP 우회
프록시 회전
브라우저 지문 위장
접근 제한 우회
```

수집 전에 각 사이트의 이용약관과 robots 정책을 확인하고 허용된 범위와 속도로만 동작시킨다.

---

# 12. 동기화 유스케이스

## 전체 흐름

```text
1. CrawlRun 생성
2. 검색 프로필 로드
3. 출처별 검색어 선택
4. 공고 참조 목록 수집
5. source_job_id 기준 중복 제거
6. 공고 상세 조회
7. SourceJobPosting 정규화
8. 상태 판정
9. 카테고리와 기술 분류
10. 콘텐츠 해시 생성
11. 기존 데이터와 비교
12. 신규·변경·상태 변경 저장
13. 스냅샷 생성
14. 기존 활성 공고 재검증
15. CrawlRun 종료
```

## 검색에서 사라진 공고 처리

검색 결과에서 사라졌다는 이유만으로 마감 처리하지 않는다. 완료된 출처 검색에서 기존 `ACTIVE` 공고가 보이지 않으면 우선 `UNKNOWN`으로 전환하고 상태 변경 이력을 저장한다.

```text
목록 미발견
    ↓
목록 미발견 → UNKNOWN
    ↓
후속 상세 URL 재조회
    ├─ 지원 가능 확인 → ACTIVE
    ├─ 마감 확인 → CLOSED
    ├─ 404 확인 → DELETED
    └─ 네트워크·파싱 실패 → UNKNOWN 유지
```

## 실행 결과 상태

```text
SUCCESS
PARTIAL_SUCCESS
FAILED
```

일부 공고 수집이 실패해도 성공한 공고는 저장한다.

---

# 13. 콘텐츠 해시와 변경 감지

다음 필드를 정규화한 후 SHA-256을 생성한다.

```text
company_name
title
detected_status
location
experience
employment_type
deadline
responsibilities
requirements
preferred_qualifications
skills
categories
```

다음 값은 해시에 포함하지 않는다.

```text
last_checked_at
last_seen_at
HTTP 응답 시간
수집 순서
추적 쿼리 파라미터
원본 HTML
```

이전 해시와 달라졌을 때만 스냅샷을 생성한다.

---

# 14. 데이터베이스

## `job_postings`

```text
id UUID PK
source VARCHAR NOT NULL
source_job_id VARCHAR NOT NULL
canonical_url TEXT NOT NULL

company_name VARCHAR NOT NULL
title VARCHAR NOT NULL

detected_status VARCHAR NOT NULL
manual_status_override VARCHAR NULL
source_status VARCHAR NULL
status_reason TEXT NULL

location_raw VARCHAR NULL
region VARCHAR NULL
city VARCHAR NULL

experience_raw VARCHAR NULL
min_experience_years INTEGER NULL
max_experience_years INTEGER NULL

employment_type VARCHAR NULL

deadline_raw VARCHAR NULL
deadline_date DATE NULL
always_open BOOLEAN NOT NULL DEFAULT FALSE

responsibilities JSONB NOT NULL
requirements JSONB NOT NULL
preferred_qualifications JSONB NOT NULL
skills JSONB NOT NULL
categories JSONB NOT NULL

posted_at TIMESTAMPTZ NULL
first_seen_at TIMESTAMPTZ NOT NULL
last_seen_at TIMESTAMPTZ NOT NULL
last_checked_at TIMESTAMPTZ NOT NULL
closed_at TIMESTAMPTZ NULL

content_hash VARCHAR(64) NOT NULL

created_at TIMESTAMPTZ NOT NULL
updated_at TIMESTAMPTZ NOT NULL
```

제약:

```sql
UNIQUE (source, source_job_id)
```

## `job_snapshots`

```text
id UUID PK
job_posting_id UUID FK NOT NULL
change_type VARCHAR NOT NULL
previous_status VARCHAR NULL
current_status VARCHAR NULL
changed_fields JSONB NOT NULL
content_hash VARCHAR(64) NOT NULL
snapshot JSONB NOT NULL
created_at TIMESTAMPTZ NOT NULL
```

## `crawl_runs`

```text
id UUID PK
source VARCHAR NOT NULL
profile_id VARCHAR NOT NULL
status VARCHAR NOT NULL

started_at TIMESTAMPTZ NOT NULL
finished_at TIMESTAMPTZ NULL

searched_count INTEGER NOT NULL DEFAULT 0
fetched_count INTEGER NOT NULL DEFAULT 0
created_count INTEGER NOT NULL DEFAULT 0
updated_count INTEGER NOT NULL DEFAULT 0
closed_count INTEGER NOT NULL DEFAULT 0
deleted_count INTEGER NOT NULL DEFAULT 0
failed_count INTEGER NOT NULL DEFAULT 0
request_count INTEGER NOT NULL DEFAULT 0

query_results JSONB NOT NULL
error_summary JSONB NOT NULL
created_at TIMESTAMPTZ NOT NULL
```

## 권장 인덱스

```text
job_postings.detected_status
job_postings.source
job_postings.first_seen_at
job_postings.updated_at
job_postings.deadline_date
job_postings.company_name
GIN job_postings.categories
GIN job_postings.skills
crawl_runs.started_at
crawl_runs.source
crawl_runs.status
```

---

# 15. OpenAPI 설계

모든 API는 `/api/v1` 아래에 둔다.

FastAPI의 자동 문서는 다음 경로로 제공한다.

```text
/docs
/redoc
/openapi.json
```

## 헬스 체크

```http
GET /health
GET /ready
```

`/health`는 프로세스 생존 여부만 확인한다.

`/ready`는 다음을 확인한다.

```text
DB 연결
마이그레이션 상태
프로필 로드 상태
```

---

## 공고 목록

```http
GET /api/v1/jobs
```

### 필터

```text
keyword
sources
statuses
categories
skills
region
min_experience
max_experience
first_seen_from
first_seen_to
updated_from
updated_to
deadline_from
deadline_to
profile
limit
cursor
sort
```

기본값:

```text
statuses=ACTIVE
limit=20
sort=first_seen_at:desc
```

예:

```http
GET /api/v1/jobs?statuses=ACTIVE&categories=CPP,SDV,AUTOMOTIVE_SECURITY&limit=30
```

### 응답

```json
{
  "items": [
    {
      "id": "0198...",
      "source": "WANTED",
      "source_job_id": "315142",
      "company_name": "회사명",
      "title": "차량 보안 C++ 개발자",
      "effective_status": "ACTIVE",
      "region": "경기",
      "experience": {
        "raw": "경력 3~8년",
        "min_years": 3,
        "max_years": 8
      },
      "deadline": {
        "raw": "상시채용",
        "date": null,
        "always_open": true
      },
      "categories": [
        "CPP",
        "MOBILITY",
        "AUTOMOTIVE_SECURITY"
      ],
      "skills": [
        "C++",
        "Linux",
        "PKI"
      ],
      "first_seen_at": "2026-08-06T02:00:00+09:00",
      "last_checked_at": "2026-08-06T03:00:00+09:00",
      "url": "https://www.wanted.co.kr/wd/315142"
    }
  ],
  "page": {
    "limit": 30,
    "next_cursor": null
  }
}
```

---

## 공고 상세

```http
GET /api/v1/jobs/{job_id}
```

## 공고 스냅샷

```http
GET /api/v1/jobs/{job_id}/snapshots
```

## 공고 변경 이력

```http
GET /api/v1/jobs/{job_id}/changes
```

## 공고 상태 재확인

관리자 인증 필요:

```http
POST /api/v1/admin/jobs/{job_id}/recheck
```

응답은 즉시 완료 결과 또는 실행 ID를 반환한다.

장시간 작업이면 `202 Accepted`를 사용한다.

---

# 16. 출처 API

## 출처 목록

```http
GET /api/v1/sources
```

응답 예:

```json
{
  "items": [
    {
      "source": "WANTED",
      "enabled": true,
      "status": "HEALTHY",
      "last_success_at": "2026-08-06T02:04:00+09:00",
      "last_failure_at": null,
      "capabilities": {
        "keyword_search": true,
        "incremental_sync": true,
        "status_check": true,
        "posted_date": false,
        "deadline": true
      },
      "settings": {
        "request_delay_seconds": 1.5,
        "max_concurrency": 2
      }
    },
    {
      "source": "SARAMIN",
      "enabled": false,
      "status": "DISABLED",
      "disabled_reason": "access key is not configured"
    }
  ]
}
```

---

# 17. 프로필 API

```http
GET /api/v1/profiles
GET /api/v1/profiles/{profile_id}
POST /api/v1/admin/profiles
PUT /api/v1/admin/profiles/{profile_id}
DELETE /api/v1/admin/profiles/{profile_id}
POST /api/v1/admin/profiles/reload
```

`POST`, `PUT`, `DELETE`는 관리자 인증이 필요하다. 프로필 ID는 생성 후 변경하지 않는다. 요청 본문은 YAML과 같은 `id`, `display_name`, `queries`, `include_keywords`, `exclude_keywords`, `source_queries` 구조를 사용한다.

---

# 18. 관리자 동기화 API

## 전체 동기화

```http
POST /api/v1/admin/sync
```

요청:

```json
{
  "profile": "mobility_sdv_security_cpp",
  "mode": "incremental"
}
```

응답:

```json
{
  "run_ids": [
    "0198..."
  ],
  "status": "STARTED"
}
```

## 특정 출처 동기화

```http
POST /api/v1/admin/sources/{source}/sync
```

## 기존 활성 공고 재확인

```http
POST /api/v1/admin/sources/{source}/recheck
```

## 수집 실행 목록

```http
GET /api/v1/admin/crawl-runs
```

## 실행 상세

```http
GET /api/v1/admin/crawl-runs/{run_id}
```

## 동시 실행 제한

동일한 `source + profile` 조합이 이미 실행 중이면:

```http
409 Conflict
```

PostgreSQL advisory lock 또는 DB lock row를 사용한다.

프로세스 내부 `asyncio.Lock`만으로 구현하지 않는다.

---

# 19. 대시보드 통계 API

```http
GET /api/v1/dashboard/summary
GET /api/v1/dashboard/job-trends
GET /api/v1/dashboard/category-distribution
GET /api/v1/dashboard/source-distribution
GET /api/v1/dashboard/crawl-health
```

## 요약 응답

```json
{
  "jobs": {
    "total": 184,
    "active": 72,
    "closed": 96,
    "unknown": 12,
    "deleted": 4,
    "new_last_7_days": 8,
    "closed_last_7_days": 3
  },
  "sources": {
    "enabled": 1,
    "healthy": 1,
    "failed": 0
  },
  "crawl": {
    "last_started_at": "2026-08-06T02:00:00+09:00",
    "last_finished_at": "2026-08-06T02:04:31+09:00",
    "last_status": "SUCCESS",
    "next_run_at": "2026-08-07T02:00:00+09:00"
  }
}
```

---

# 20. 관리자 인증

MVP는 단일 관리자 API 키를 사용한다.

```http
Authorization: Bearer ${ADMIN_API_KEY}
```

적용 대상:

```text
/api/v1/admin/*
```

대시보드 로그인 화면에서 API 키를 입력받고 `sessionStorage`에만 저장한다.

금지:

```text
localStorage 저장
URL 쿼리로 API 키 전달
로그에 API 키 출력
응답에 API 키 반환
```

서버에서는 안전한 상수 시간 비교를 사용한다.

운영 환경에서는 HTTPS 또는 신뢰 가능한 리버스 프록시 뒤에서 사용한다.

---

# 21. 대시보드 화면

## 개요 `/`

표시:

```text
전체 공고 수
활성 공고 수
최근 7일 신규 공고
최근 7일 마감 공고
상태 확인 불가 공고
최근 동기화 시각
활성 출처 수
최근 실패 수
```

차트:

```text
최근 30일 신규·마감 추이
출처별 활성 공고 수
카테고리별 활성 공고 수
최근 수집 성공률
```

---

## 공고 목록 `/jobs`

필터:

```text
검색어
출처
상태
카테고리
기술
지역
경력
최초 발견 기간
마감일
```

컬럼:

```text
회사
포지션
출처
상태
카테고리
기술
지역
경력
마감일
최초 발견
마지막 확인
```

기능:

```text
서버 페이지네이션
서버 정렬
행 클릭 상세 이동
원본 공고 새 창 열기
CSV 내보내기
공고 재확인
```

기본 상태 필터는 `ACTIVE`다.

---

## 공고 상세 `/jobs/:id`

표시:

```text
회사명
포지션명
출처
출처 공고 ID
원본 URL
상태
상태 판정 근거
지역
경력
고용 형태
게시일
마감일
주요 업무
자격요건
우대사항
기술
카테고리
최초 발견
마지막 발견
마지막 확인
마감 확인
```

변경 이력:

```text
시각
변경 유형
이전 상태
현재 상태
변경된 필드
```

---

## 수집 실행 `/crawl-runs`

표시:

```text
출처
프로필
상태
시작 시각
종료 시각
검색 결과 수
상세 조회 수
신규
변경
마감
삭제
실패
요청 수
```

상세 `/crawl-runs/:id`:

```text
실행 파라미터
검색어별 결과
HTTP 상태 코드 집계
재시도 횟수
실패 URL
파서 오류
오류 요약
```

원본 HTML은 표시하지 않는다.

---

## 출처 `/sources`

출처별 카드:

```text
활성화 여부
현재 상태
마지막 성공
마지막 실패
수집 지연
동시 요청 수
지원 기능
최근 오류
```

기능:

```text
수동 동기화
기존 활성 공고 재확인
```

---

## 프로필 `/profiles`

표시 및 관리:

```text
프로필 ID
표시 이름
검색어
사이트별 검색어
포함 키워드
제외 키워드
마지막 실행
활성 공고 수
프로필 추가
프로필 수정
프로필 삭제
```

추가·수정·삭제에는 대시보드 세션에 저장된 관리자 API 키가 필요하다. 삭제는 DB에서 프로필을 제거하며 기존 수집 실행 이력은 유지한다.

---

## 설정 `/settings`

표시:

```text
애플리케이션 버전
실행 환경
서버 시간대
DB 연결 상태
마이그레이션 버전
다음 예약 실행
활성화된 출처
환경변수 설정 여부
```

비밀값은 마스킹한다.

```text
ADMIN_API_KEY: 설정됨
SARAMIN_ACCESS_KEY: 설정되지 않음
```

---

# 22. 스케줄러

API 프로세스와 스케줄러를 분리한다.

기본 일정:

```text
매일 02:00 검색 프로필 동기화
매일 03:00 기존 ACTIVE 공고 상태 재검증
```

기본 일정은 환경변수로 시드한다. 이후 대시보드 설정 화면에서 Cron 형식으로 변경하며, 변경값은 PostgreSQL에 저장되고 Scheduler가 최대 30초 안에 적용한다.

API 컨테이너에서는 스케줄러를 실행하지 않는다.

여러 Uvicorn worker로 인해 작업이 중복 실행될 수 있기 때문이다.

---

# 23. 백엔드 설정

```python
from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=True,
        extra="ignore",
    )

    app_name: str = Field(default="job-collector", alias="APP_NAME")
    app_env: str = Field(default="production", alias="APP_ENV")
    app_version: str = Field(default="0.1.0", alias="APP_VERSION")
    log_level: str = Field(default="INFO", alias="LOG_LEVEL")
    timezone: str = Field(default="Asia/Seoul", alias="TZ")

    database_url: str = Field(alias="DATABASE_URL")
    admin_api_key: str = Field(alias="ADMIN_API_KEY")

    wanted_enabled: bool = Field(default=True, alias="WANTED_ENABLED")
    wanted_base_url: str = Field(
        default="https://www.wanted.co.kr",
        alias="WANTED_BASE_URL",
    )
    wanted_request_delay_seconds: float = Field(
        default=1.5,
        alias="WANTED_REQUEST_DELAY_SECONDS",
    )
    wanted_max_concurrency: int = Field(
        default=2,
        alias="WANTED_MAX_CONCURRENCY",
    )

    saramin_enabled: bool = Field(default=False, alias="SARAMIN_ENABLED")
    saramin_access_key: str | None = Field(
        default=None,
        alias="SARAMIN_ACCESS_KEY",
    )

    jobkorea_enabled: bool = Field(
        default=False,
        alias="JOBKOREA_ENABLED",
    )

    http_timeout_seconds: float = Field(
        default=20.0,
        alias="HTTP_TIMEOUT_SECONDS",
    )
    http_max_retries: int = Field(
        default=3,
        alias="HTTP_MAX_RETRIES",
    )

    scheduler_enabled: bool = Field(
        default=False,
        alias="SCHEDULER_ENABLED",
    )
    sync_cron: str = Field(
        default="0 2 * * *",
        alias="SYNC_CRON",
    )
    recheck_cron: str = Field(
        default="0 3 * * *",
        alias="RECHECK_CRON",
    )

    default_profile: str = Field(
        default="mobility_sdv_security_cpp",
        alias="DEFAULT_PROFILE",
    )
```

---

# 24. `.env.example`

```dotenv
APP_NAME=job-collector
APP_ENV=production
APP_VERSION=0.1.0
LOG_LEVEL=INFO
TZ=Asia/Seoul

POSTGRES_DB=job_collector
POSTGRES_USER=job_collector
POSTGRES_PASSWORD=change-me

DATABASE_URL=postgresql+asyncpg://job_collector:change-me@postgres:5432/job_collector

ADMIN_API_KEY=replace-with-a-long-random-secret

WANTED_ENABLED=true
WANTED_BASE_URL=https://www.wanted.co.kr
WANTED_REQUEST_DELAY_SECONDS=1.5
WANTED_MAX_CONCURRENCY=2

SARAMIN_ENABLED=false
SARAMIN_ACCESS_KEY=

JOBKOREA_ENABLED=false

HTTP_TIMEOUT_SECONDS=20
HTTP_MAX_RETRIES=3

SCHEDULER_ENABLED=false
SYNC_CRON=0 2 * * *
RECHECK_CRON=0 3 * * *

DEFAULT_PROFILE=mobility_sdv_security_cpp

API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=2

DASHBOARD_PORT=8080
CORS_ORIGINS=http://localhost:5173
```

실제 `.env`는 Git에 커밋하지 않는다.

---

# 25. 백엔드 Dockerfile

`backend/Dockerfile`

```dockerfile
FROM python:3.13-slim AS builder

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /build

RUN apt-get update \
    && apt-get install --no-install-recommends -y build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml README.md ./
COPY src ./src

RUN python -m venv /opt/venv \
    && /opt/venv/bin/pip install --no-cache-dir --upgrade pip \
    && /opt/venv/bin/pip install --no-cache-dir .


FROM python:3.13-slim AS runtime

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1
ENV PATH="/opt/venv/bin:${PATH}"

RUN groupadd --system --gid 10001 app \
    && useradd --system --uid 10001 --gid app --create-home app

WORKDIR /app

COPY --from=builder /opt/venv /opt/venv
COPY alembic.ini ./
COPY migrations ./migrations
COPY profiles ./profiles
COPY scripts ./scripts

RUN chmod +x /app/scripts/entrypoint.sh \
    && chown -R app:app /app

USER app

EXPOSE 8000

ENTRYPOINT ["/app/scripts/entrypoint.sh"]
CMD ["api"]
```

---

# 26. 백엔드 Entrypoint

`backend/scripts/entrypoint.sh`

```sh
#!/bin/sh

set -eu

MODE="${1:-api}"

case "${MODE}" in
    api)
        exec uvicorn job_collector.adapter.inbound.http.app:create_app \
            --factory \
            --host "${API_HOST:-0.0.0.0}" \
            --port "${API_PORT:-8000}" \
            --workers "${API_WORKERS:-2}"
        ;;

    scheduler)
        exec python -m job_collector.main scheduler
        ;;

    migrate)
        exec alembic upgrade head
        ;;

    *)
        exec "$@"
        ;;
esac
```

API 시작 시 자동 마이그레이션하지 않는다.

별도 `migrate` 서비스가 수행한다.

---

# 27. 대시보드 Dockerfile

`dashboard/Dockerfile`

```dockerfile
FROM node:22-alpine AS builder

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

ARG VITE_API_BASE_URL=/api
ENV VITE_API_BASE_URL=${VITE_API_BASE_URL}

RUN npm run build


FROM nginx:alpine

COPY nginx/default.conf /etc/nginx/conf.d/default.conf
COPY --from=builder /app/dist /usr/share/nginx/html

EXPOSE 80
```

---

# 28. Nginx 설정

`dashboard/nginx/default.conf`

```nginx
server
{
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /api/
    {
        proxy_pass http://api:8000/api/;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 10s;
        proxy_read_timeout 120s;
    }

    location /health
    {
        proxy_pass http://api:8000/health;
    }

    location /docs
    {
        proxy_pass http://api:8000/docs;
    }

    location /openapi.json
    {
        proxy_pass http://api:8000/openapi.json;
    }

    location /
    {
        try_files $uri $uri/ /index.html;
    }
}
```

---

# 29. Docker Compose

`compose.yml`

```yaml
services:
    postgres:
        image: postgres:17-alpine
        container_name: job-collector-postgres
        restart: unless-stopped
        environment:
            TZ: ${TZ:-Asia/Seoul}
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        volumes:
            - ./data/postgres:/var/lib/postgresql/data
        healthcheck:
            test:
                - CMD-SHELL
                - pg_isready -U "$${POSTGRES_USER}" -d "$${POSTGRES_DB}"
            interval: 10s
            timeout: 5s
            retries: 10
        networks:
            - job-collector-net

    migrate:
        build:
            context: ./backend
            dockerfile: Dockerfile
        image: job-collector-backend:local
        container_name: job-collector-migrate
        command:
            - migrate
        env_file:
            - .env
        depends_on:
            postgres:
                condition: service_healthy
        restart: "no"
        networks:
            - job-collector-net

    api:
        image: job-collector-backend:local
        container_name: job-collector-api
        restart: unless-stopped
        command:
            - api
        env_file:
            - .env
        expose:
            - "8000"
        depends_on:
            postgres:
                condition: service_healthy
            migrate:
                condition: service_completed_successfully
        healthcheck:
            test:
                - CMD
                - python
                - -c
                - |
                  import urllib.request
                  urllib.request.urlopen("http://127.0.0.1:8000/health", timeout=5)
            interval: 30s
            timeout: 5s
            retries: 5
            start_period: 20s
        networks:
            - job-collector-net

    scheduler:
        image: job-collector-backend:local
        container_name: job-collector-scheduler
        restart: unless-stopped
        command:
            - scheduler
        env_file:
            - .env
        environment:
            SCHEDULER_ENABLED: "true"
        depends_on:
            postgres:
                condition: service_healthy
            migrate:
                condition: service_completed_successfully
        networks:
            - job-collector-net

    dashboard:
        build:
            context: ./dashboard
            dockerfile: Dockerfile
            args:
                VITE_API_BASE_URL: /api
        image: job-collector-dashboard:local
        container_name: job-collector-dashboard
        restart: unless-stopped
        ports:
            - "${DASHBOARD_PORT:-8080}:80"
        depends_on:
            api:
                condition: service_healthy
        healthcheck:
            test:
                - CMD
                - wget
                - --quiet
                - --tries=1
                - --spider
                - http://127.0.0.1/
            interval: 30s
            timeout: 5s
            retries: 5
        networks:
            - job-collector-net

networks:
    job-collector-net:
        name: job-collector-net
```

외부에는 대시보드만 공개한다.

```text
http://서버주소:8080
```

API 직접 접근이 필요한 개발 환경에서만 다음을 임시 추가한다.

```yaml
ports:
    - "8000:8000"
```

---

# 30. 로깅

JSON 구조화 로그를 사용한다.

필수 필드:

```text
timestamp
level
event
request_id
source
profile_id
crawl_run_id
source_job_id
duration_ms
status_code
error_type
```

금지:

```text
API 키 출력
원본 HTML 전체 출력
DB 비밀번호 출력
Authorization 헤더 출력
```

---

# 31. 오류 모델

## 외부 출처 오류

```text
SourceRateLimitedError
SourceAccessDeniedError
SourceNotFoundError
SourceParseError
SourceTemporaryError
SourceConfigurationError
```

## HTTP 상태 코드

```text
400 잘못된 검색 조건
401 관리자 인증 누락
403 관리자 인증 실패
404 공고·프로필·실행 기록 없음
409 동일 동기화 실행 중
422 요청 모델 검증 실패
503 DB 또는 필수 내부 의존성 장애
```

한 채용 사이트의 수집 장애만으로 공고 조회 API가 `503`을 반환하면 안 된다.

---

# 32. 테스트

## 도메인 단위 테스트

```text
명시적 마감 상태 판정
미래 마감일 활성 판정
지난 마감일 종료 판정
상시채용 판정
지원 버튼 활성 판정
UNKNOWN 판정
경력 범위 파싱
카테고리 분류
콘텐츠 해시 안정성
변경 유형 판정
```

## 원티드 파서 fixture

```text
활성 공고
상시채용 공고
마감 공고
삭제 공고
필드 일부 누락
JSON-LD 포함
구조화 데이터 없음
지원 버튼 없음
```

## 어댑터 계약 테스트

```python
class JobSourceContract:
    async def test_search_returns_unique_references(self):
        ...

    async def test_fetch_detail_preserves_source_identity(self):
        ...

    async def test_missing_fields_are_not_invented(self):
        ...

    async def test_closed_job_is_not_active(self):
        ...

    async def test_unknown_status_is_preserved(self):
        ...
```

## 통합 테스트

```text
신규 공고 저장
동일 공고 재수집 시 중복 방지
내용 변경 시 스냅샷 생성
ACTIVE → CLOSED
CLOSED → ACTIVE
ACTIVE → DELETED
공고 필터 조합
커서 페이지네이션
관리자 인증
동시 동기화 409
대시보드 집계
```

## 프론트엔드 테스트

```text
관리자 로그인
세션 토큰 저장
인증 실패 처리
공고 목록 필터
서버 페이지네이션
상태 배지
공고 상세
변경 이력
수동 동기화
실행 중 버튼 비활성화
빈 결과
API 오류
```

---

# 33. 구현 순서

Agent는 다음 순서로 구현한다.

```text
1. 모노레포와 기본 설정 생성
2. 백엔드 도메인 모델 구현
3. inbound·outbound 포트 정의
4. SQLAlchemy 모델 구현
5. Alembic 초기 마이그레이션
6. Repository 구현
7. 검색 프로필 로더 구현
8. 정규화·분류·해시·변경 감지 구현
9. 원티드 fixture 확보
10. 원티드 파서 단위 테스트 작성
11. 원티드 Client·Parser·Mapper·Adapter 구현
12. 동기화 유스케이스 구현
13. 공고 조회 OpenAPI 구현
14. 관리자 동기화 OpenAPI 구현
15. 출처·프로필 API 구현
16. 대시보드 통계 API 구현
17. 관리자 인증 구현
18. 스케줄러 구현
19. React 대시보드 레이아웃 구현
20. 개요 화면 구현
21. 공고 목록·상세 구현
22. 수집 실행 목록·상세 구현
23. 출처·프로필·설정 화면 구현
24. 수동 동기화·재확인 구현
25. 백엔드·프론트엔드 테스트
26. Dockerfile·Compose 구현
27. README와 운영 문서 작성
28. 사람인 공식 API 어댑터 추가
29. 잡코리아 수집 방식 검토 후 어댑터 추가
```

---

# 34. MVP 완료 기준

다음 조건을 모두 만족해야 한다.

```text
Docker Compose로 PostgreSQL·API·Scheduler·Dashboard 실행 가능
Alembic 마이그레이션이 별도 서비스로 선행됨
/docs와 /openapi.json 제공
원티드 공고 검색과 상세 수집 가능
ACTIVE·CLOSED·UNKNOWN·DELETED 구분 가능
기본 공고 목록은 ACTIVE만 반환
마감 공고가 활성 목록에 포함되지 않음
동일 공고 중복 저장 방지
내용 변경 시 스냅샷 생성
공고 목록 필터·정렬·페이지네이션 제공
공고 상세와 변경 이력 제공
대시보드에서 통계 확인 가능
대시보드에서 공고 검색 가능
대시보드에서 수집 실행 이력 확인 가능
대시보드에서 수동 동기화 가능
동일 동기화 중복 실행 방지
출처 하나가 실패해도 전체 API는 계속 동작
기본 검색 프로필을 YAML에서 시드하고 DB에서 관리
새 채용 사이트는 JobSourcePort 구현으로 추가 가능
원티드 파서 fixture 테스트 존재
어댑터 계약 테스트 존재
리서치 도구·LLM·알림 관련 코드는 포함되지 않음
README에 설치·실행·동기화·API 사용 방법 작성
```

이 프로젝트의 최종 산출물은 **채용 공고를 수집하고 관리하며, 표준화된 OpenAPI와 관리자 대시보드를 제공하는 독립 서비스**다.
